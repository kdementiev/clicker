{% extends '::base.html.twig' %}

{% block content %}
    <div class="panel panel-success">
        <div class="panel-heading">
            <h3 class="panel-title">{{ 'page.click.success.title'|trans }}</h3>
        </div>

        <div class="panel-body">
            You click #[{{ click.id }}] <b>passed</b>!
        </div>
    </div>

    <a href="{{ path('link_index') }}" class="btn btn-info bottom-margin">{{ 'page.click.success.back'|trans }}</a>
{% endblock %}